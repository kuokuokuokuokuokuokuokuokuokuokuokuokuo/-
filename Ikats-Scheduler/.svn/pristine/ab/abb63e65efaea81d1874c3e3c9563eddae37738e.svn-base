import api from '../Common/WebAPI';

export default class LoginService
{
    static conLogin(data)
    {
        let result = api("/Scheduler-Web/User/login.action",data);
        return result;
    }
}